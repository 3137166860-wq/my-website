---
const { title } = Astro.props;
---

<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{title}</title>
    <style is:global>
        :root {
            --bg-color: #050508; 
            --gradient-blue: linear-gradient(135deg, #0a192f 0%, #112240 100%); 
            --text-main: #ccd6f6; 
            --text-muted: #8892b0; 
            --accent-color: #64ffda; 
        }

        body {
            margin: 0; padding: 0;
            font-family: 'SF Pro Display', -apple-system, BlinkMacSystemFont, sans-serif;
            background-color: var(--bg-color);
            color: var(--text-main);
            line-height: 1.6;
        }

        .container { max-width: 800px; margin: 0 auto; padding: 40px 20px; }

        /* --- 新增：导航栏样式 --- */
        nav {
            display: flex;
            gap: 20px;
            margin-bottom: 40px;
            padding: 15px 0;
            border-bottom: 1px solid rgba(100, 255, 218, 0.1);
        }
        nav a {
            color: var(--text-main);
            text-decoration: none;
            font-size: 0.9rem;
            letter-spacing: 1px;
            transition: color 0.3s;
        }
        nav a:hover {
            color: var(--accent-color);
        }
        /* ----------------------- */

        .hero { padding: 60px 0; border-bottom: 1px solid rgba(255, 255, 255, 0.05); }
        .hero h1 { font-size: 3rem; margin: 0; background: linear-gradient(90deg, #fff, var(--text-muted)); -webkit-background-clip: text; -webkit-text-fill-color: transparent; }
        .section { background: var(--gradient-blue); border-radius: 16px; padding: 30px; margin-top: 40px; border: 1px solid rgba(255,255,255,0.05); }
        .section h2 { color: var(--text-main); display: flex; align-items: center; margin-top: 0; }
        .section h2::before { content: "/>"; color: var(--accent-color); margin-right: 10px; font-family: monospace; }
        .tags { display: flex; gap: 10px; flex-wrap: wrap; }
        .tag { background: rgba(100, 255, 218, 0.1); color: var(--accent-color); padding: 5px 15px; border-radius: 20px; font-size: 0.8rem; }
        button { background: transparent; color: var(--accent-color); border: 1px solid var(--accent-color); padding: 10px 25px; border-radius: 4px; cursor: pointer; transition: all 0.3s; }
        button:hover { background: rgba(100, 255, 218, 0.1); }
        .msg-item { background: rgba(0, 0, 0, 0.2); padding: 15px; border-radius: 8px; margin-top: 15px; border-left: 3px solid var(--text-muted); }
    </style>
</head>
<body>
    <div class="container">
        <nav>
            <a href="/">HOME // 首页</a>
            <a href="/games">TERMINAL // 游戏</a>
            <a href="/blog">BLOG // 博客</a>
        </nav>

        <slot />
    </div>
</body>
</html>